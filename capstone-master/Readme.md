### Backend development

1.create git repo
     git init ==> .git folder is created and it is hidden
    
2.Add .gitignore file ==>  to store the files that are not to be pushed into github

3.create .env file ==>  for the envinormental variables and read data from .env with 'dotenv' module 
      npm install dotenv

4.Generate package.json
     npm init -y //change type to module

5.create express
   npm i express

6. create server.js file =>import express and mongoose

7.connect to database
       install mongoose =>npm i mongoose

8.add middlewares(body parser ,error handler )

9.Design schemas and create models

10.design rest api's for all resources

### Registration and login
11. registration 





### template literal
console.log(`a is{$a} b is {$b})